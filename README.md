# Innovation-Day-VR-Sprint-Board

## Getting Started
1. Clone the repo to your machine and `cd` into it
2. In a separate terminal tab/window, run `python -m SimpleHTTPServer 8000`
3. In a separate terminal tab/window, run `./ngrok http 8000`